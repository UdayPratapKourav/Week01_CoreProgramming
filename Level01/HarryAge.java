import java.util.*;  // Importing the util package

class HarryAge {
    public static void main(String[] args) {
        // Declare Harry's birth year and the current year
        int birthYear = 2000;
        int currentYear = 2024;

        // Calculate Harry's current age
        int currentAge = currentYear - birthYear;

        // Print Harry's age in 2024
        System.out.println("Harry's age in "+currentYear+ " is " + currentAge);
    }
}
